import MainNavigation from '../MainNavigation';
import { Header } from './AppBar.styled';

const AppBar = () => {
  return (
    <Header>
      <MainNavigation />
    </Header>
  );
};
export default AppBar;